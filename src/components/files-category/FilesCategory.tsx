import { PostType } from '../../types/post.type'

type Props = {
  postsList: PostType[]
}

const FilesCategory = ({ postsList }: Props) => {
  return <div>FilesCategory</div>
}

export default FilesCategory
